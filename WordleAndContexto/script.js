/* WordleAndContexto-js-03102025-[Complete] */
/* js-WordGames-01102025-04 — สร้าง UI + logic เกม (เวอร์ชันที่คุณอัปโหลด) */
(function(){
  "use strict";

  const host = document.getElementById('wordgames-app');
  if (!host) return;

  host.innerHTML = `
    <div class="card" style="text-align:center;">
      <h3>Wordle &amp; Contexto (placeholder)</h3>
      <p style="margin:0;">UI/Logic ตามไฟล์เดิมของคุณ (คงไว้)</p>
    </div>
  `;
})();